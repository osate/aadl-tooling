# Pin the public npm registry so `resolved` URLs in package-lock.json stay
# publicly reachable. Without this, a developer whose user-level npm config
# points at a private mirror rewrites the lockfile to that mirror's URLs and
# breaks `npm ci` for everyone else.
registry=https://registry.npmjs.org/
